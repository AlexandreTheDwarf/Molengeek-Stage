<odoo>
    <record id="view_res_partner_tree" model="ir.ui.view">
        <field name="name">res.partner.tree</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="email"/>
                <field name="participant_tag_ids" widget="many2many_tags"/>
            </tree>
        </field>
    </record>

    <record id="view_res_partner_search" model="ir.ui.view">
        <field name="name">res.partner.search</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <search>
                <filter string="Débutants" name="debutants" domain="[('participant_tag_ids.name', '=', 'Débutants')]"/>
                <filter string="Confirmés" name="confirmes" domain="[('participant_tag_ids.name', '=', 'Confirmés')]"/>
            </search>
        </field>
    </record>
</odoo>
